# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# Environments
.venv/
venv/
env/

# OS
.DS_Store

# Local caches / bulk data
*.duckdb
*.parquet

# Note: schemas/ (shipped JSON-Schema contract) and reports/ (demo outputs) are
# committed. Regenerate with `cauala export-schemas` / `cauala appraise --out`.

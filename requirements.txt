# Runtime dependencies. Prefer `uv pip install -e .` (reads pyproject.toml);
# this file is provided for plain-pip users.
pydantic>=2.5
pyyaml>=6.0
httpx>=0.27

# web app (cauala serve)
fastapi>=0.110
uvicorn>=0.29

# dev / test
pytest>=8.0
